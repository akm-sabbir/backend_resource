1) to run merge sort required to call following function:
	main(list_of_data,compare_function) such as main (a,le)
    i have two predefined function le and strLe for comparision. le should be passed if list contains integer otherwise strLe to compare string or char.
2) to run insertion sort required to call insertionSort([],list_of_data,comparision_function) such as insertionSort([],a,le)
	similarly it has two different comparision function defined such as le and strLe.
3). just load the files using use command and run the corresponding function.